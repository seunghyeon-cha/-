# Smartrip Backend 환경 변수 설정 예시
# 이 파일을 .env로 복사하여 실제 값을 입력하세요

# 서버 설정
NODE_ENV=development
PORT=4000

# 데이터베이스 설정
# 개발: SQLite 파일 경로
# 프로덕션: 영구 스토리지 경로 사용
DATABASE_PATH=./data/smartrip.db

# JWT 인증
# 프로덕션에서는 반드시 강력한 랜덤 문자열로 변경하세요
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# 한국관광공사 Tour API
# https://www.data.go.kr 에서 발급
TOUR_API_KEY=your-tour-api-key

# CORS 설정
# 프론트엔드 도메인을 추가하세요
CORS_ORIGINS=http://localhost:3000,https://smartrip.com

# 프로덕션 설정 예시
# NODE_ENV=production
# PORT=4000
# DATABASE_PATH=/app/data/smartrip.db
# JWT_SECRET=<32자 이상의 랜덤 문자열>
# TOUR_API_KEY=<공공데이터포털 API 키>
# CORS_ORIGINS=https://smartrip.com,https://www.smartrip.com
