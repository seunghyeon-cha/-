# Phase 8: 사업자 페이지 라우팅 구조

## 현재 상태 분석

### 기존 Business 페이지
```
/src/app/(main)/business/
├── verify/
│   └── page.tsx          # 사업자 인증 신청/상태 확인
└── stats/
    └── page.tsx          # 통계 대시보드
```

### 기존 페이지 분석

#### 1. `/business/verify` - 사업자 인증 페이지
- **목적**: 사업자 인증 신청 및 상태 확인
- **주요 기능**:
  - 사업자 등록번호, 상호명, 대표자명, 주소 입력
  - 사업자등록증 및 신분증 이미지 URL 제출
  - 인증 상태 확인 (PENDING, APPROVED, REJECTED)
  - 승인 후 `/business/places`로 이동 버튼 제공
- **코드 라인**: 357줄
- **사용 API**: `getVerificationStatus()`, `createVerification()`

#### 2. `/business/stats` - 통계 대시보드
- **목적**: 전체 사업 통계 요약 및 업장별 통계
- **주요 기능**:
  - 총 업장 수, 평균 평점, 총 리뷰 수, 총 북마크 수 카드
  - 업장별 통계 테이블 (평점, 리뷰, 북마크)
  - 업장 보기/수정 링크
- **코드 라인**: 251줄
- **사용 API**: `getBusinessStatsSummary()`, `getPlaceStats()`

---

## Phase 8 라우팅 설계

### 새로 구현할 페이지

```
/src/app/(main)/business/
├── layout.tsx            # 🆕 사업자 페이지 공통 레이아웃 (네비게이션)
├── verify/
│   └── page.tsx          # ✅ 기존 - 사업자 인증
├── stats/
│   └── page.tsx          # ✅ 기존 - 통계 대시보드
├── places/
│   ├── page.tsx          # 🆕 업장 목록 및 관리
│   └── [id]/
│       └── page.tsx      # 🆕 업장 상세 통계
└── promotions/
    └── page.tsx          # 🆕 프로모션 관리
```

---

## 페이지별 상세 설계

### 1. `/business/layout.tsx` - 공통 레이아웃 (새로 구현)

**목적**: 사업자 페이지 전체에 일관된 네비게이션 제공

**구조**:
```typescript
- Sidebar Navigation (sticky)
  - 대시보드 (/business/stats)
  - 업장 관리 (/business/places)
  - 프로모션 관리 (/business/promotions)
  - 인증 관리 (/business/verify)
```

**참고 구현**: `/src/app/(main)/mypage/layout.tsx` (108줄)

**주요 기능**:
- Sticky sidebar with active route highlighting
- Section-based grouping
- Responsive grid layout (lg:grid-cols-4)

---

### 2. `/business/places/page.tsx` - 업장 목록 및 관리 (새로 구현)

**목적**: 사업자가 소유한 모든 업장을 관리

**사용 API**:
- `getMyPlaces()` - 내 업장 목록 조회

**주요 기능**:
1. **업장 카드 그리드**
   - 3-column responsive grid
   - 업장 이미지, 이름, 주소, 카테고리
   - 평균 평점 (avgRating)
   - 리뷰 수 (reviewCount)
   - 북마크 수 (bookmarkCount)

2. **액션 버튼**
   - 상세 통계 보기 → `/business/places/[id]`
   - 프로모션 관리 → `/business/promotions?placeId=[id]`
   - 업장 정보 수정 → `/places/[id]/edit`

3. **빈 상태 처리**
   - 업장이 없을 경우 안내 메시지
   - 업장 등록 버튼 (아직 구현 안됨 - 향후 작업)

**UI 구성**:
```
┌─────────────────────────────────────────┐
│ 헤더                                     │
│ - 제목: "내 업장 관리"                   │
│ - 총 업장 수                             │
│ - [업장 등록] 버튼 (비활성 - 향후 구현)  │
└─────────────────────────────────────────┘

┌──────────┐ ┌──────────┐ ┌──────────┐
│ 업장 카드 │ │ 업장 카드 │ │ 업장 카드 │
│ - 이미지  │ │ - 이미지  │ │ - 이미지  │
│ - 이름    │ │ - 이름    │ │ - 이름    │
│ - 주소    │ │ - 주소    │ │ - 주소    │
│ - 평점    │ │ - 평점    │ │ - 평점    │
│ - 통계    │ │ - 통계    │ │ - 통계    │
│ [액션]    │ │ [액션]    │ │ [액션]    │
└──────────┘ └──────────┘ └──────────┘
```

**예상 코드 라인**: 약 250-300줄

---

### 3. `/business/places/[id]/page.tsx` - 업장 상세 통계 (새로 구현)

**목적**: 특정 업장의 상세 통계 및 평점 분포 확인

**사용 API**:
- `getPlaceDetailStats(placeId)` - 업장 상세 통계

**주요 기능**:
1. **업장 기본 정보**
   - 업장명, 주소, 카테고리
   - 평균 평점, 총 리뷰 수, 총 북마크 수

2. **평점 분포 차트**
   - 5점부터 1점까지 각 평점별 리뷰 수
   - 막대 그래프 또는 프로그레스 바로 시각화
   - 백분율 표시

3. **최근 리뷰 목록**
   - 최근 작성된 리뷰 5개
   - 작성자, 평점, 내용, 작성일

4. **액션 버튼**
   - 전체 리뷰 보기 → `/places/[id]#reviews`
   - 프로모션 관리 → `/business/promotions?placeId=[id]`
   - 업장 정보 수정 → `/places/[id]/edit`

**UI 구성**:
```
┌─────────────────────────────────────────┐
│ 업장 기본 정보                           │
│ - 이름, 주소, 카테고리                   │
│ - 평균 평점 ★ 4.2                       │
│ - 리뷰 145개 | 북마크 89개               │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 평점 분포                                │
│ ★★★★★ [████████████] 80 (55%)         │
│ ★★★★☆ [██████    ] 40 (28%)           │
│ ★★★☆☆ [███       ] 15 (10%)           │
│ ★★☆☆☆ [██        ] 8  (6%)            │
│ ★☆☆☆☆ [█         ] 2  (1%)            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 최근 리뷰                                │
│ - 리뷰 카드 x 5개                        │
└─────────────────────────────────────────┘
```

**예상 코드 라인**: 약 300-350줄

---

### 4. `/business/promotions/page.tsx` - 프로모션 관리 (새로 구현)

**목적**: 프로모션 생성, 수정, 삭제, 활성화/비활성화 관리

**사용 API**:
- `getMyPromotions()` - 전체 프로모션 목록
- `getPromotionsByPlace(placeId, includeInactive)` - 업장별 프로모션 (필터링용)
- `createPromotion(placeId, data)` - 프로모션 생성
- `updatePromotion(id, data)` - 프로모션 수정
- `deletePromotion(id)` - 프로모션 삭제
- `togglePromotionStatus(id)` - 활성화/비활성화 토글

**주요 기능**:
1. **필터 및 검색**
   - 업장별 필터 (드롭다운)
   - 상태 필터 (전체/진행중/예정/종료)
   - 활성 상태 필터 (전체/활성/비활성)

2. **프로모션 목록 테이블**
   - 제목, 업장명, 기간, 상태, 활성 여부
   - 진행 상태 뱃지:
     - 🟢 진행중 (startDate ≤ today ≤ endDate && isActive)
     - 🟡 예정 (startDate > today)
     - 🔴 종료 (endDate < today)
   - 활성화 토글 스위치

3. **액션**
   - [+ 프로모션 생성] 버튼 → 모달 오픈
   - 수정 버튼 → 모달 오픈
   - 삭제 버튼 → 확인 후 삭제
   - 활성화 토글 → 즉시 API 호출

4. **생성/수정 모달**
   - 업장 선택 (드롭다운 - 생성 시에만)
   - 제목 (필수)
   - 설명 (필수)
   - 시작일 (필수)
   - 종료일 (필수)
   - 활성화 여부 (체크박스)

**UI 구성**:
```
┌─────────────────────────────────────────┐
│ 헤더                                     │
│ - 제목: "프로모션 관리"                  │
│ - [+ 프로모션 생성] 버튼                 │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 필터                                     │
│ [업장 선택 ▼] [상태 ▼] [활성 ▼]         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 프로모션 테이블                          │
│ ┌──────────┬────────┬────────┬────────┐ │
│ │ 제목     │ 업장   │ 기간   │ 상태   │ │
│ ├──────────┼────────┼────────┼────────┤ │
│ │ 봄 할인  │ 카페A  │ 3/1~.. │🟢진행중│ │
│ │          │        │        │[토글]  │ │
│ │          │        │  [수정] [삭제]  │ │
│ └──────────┴────────┴────────┴────────┘ │
└─────────────────────────────────────────┘
```

**프로모션 상태 로직**:
```typescript
const getPromotionStatus = (startDate: string, endDate: string, isActive: boolean) => {
  const now = new Date();
  const start = new Date(startDate);
  const end = new Date(endDate);

  if (end < now) return { label: '종료', color: 'red' };
  if (start > now) return { label: '예정', color: 'yellow' };
  if (isActive) return { label: '진행중', color: 'green' };
  return { label: '일시중지', color: 'gray' };
};
```

**예상 코드 라인**: 약 400-450줄

---

## 구현 순서

### Phase 1: 레이아웃 설정 (우선순위: High)
1. `/business/layout.tsx` 생성
   - Sidebar navigation 구현
   - 기존 페이지와 새 페이지 모두 적용

### Phase 2: 업장 관리 (우선순위: High)
2. `/business/places/page.tsx` 구현
   - 업장 목록 그리드
   - 통계 표시
   - 액션 버튼

3. `/business/places/[id]/page.tsx` 구현
   - 상세 통계
   - 평점 분포 차트
   - 최근 리뷰

### Phase 3: 프로모션 관리 (우선순위: High)
4. `/business/promotions/page.tsx` 구현
   - 프로모션 목록 테이블
   - 필터링
   - 생성/수정/삭제 기능
   - 활성화 토글

---

## API 매핑 확인

### 업장 관리 API ✅
- ✅ `getMyPlaces()` - MyPlace[] (avgRating, reviewCount, bookmarkCount 포함)
- ✅ `getPlaceDetailStats(placeId)` - 상세 통계 및 평점 분포

### 프로모션 관리 API ✅
- ✅ `getMyPromotions()` - 전체 프로모션 목록
- ✅ `getPromotionsByPlace(placeId, includeInactive)` - 업장별 필터링
- ✅ `createPromotion(placeId, data)` - 생성
- ✅ `updatePromotion(id, data)` - 수정
- ✅ `deletePromotion(id)` - 삭제
- ✅ `togglePromotionStatus(id)` - 토글

**결론**: 모든 필요한 API가 준비되어 있음!

---

## 디자인 시스템 일관성

### Phase 7 (Mypage)와의 일관성 유지
- ✅ 동일한 레이아웃 구조 (sidebar + content)
- ✅ 동일한 카드 스타일 (rounded-lg, shadow-sm)
- ✅ 동일한 색상 시스템 (primary-600, gray-50)
- ✅ 동일한 로딩 스켈레톤
- ✅ 동일한 빈 상태 UI
- ✅ 동일한 토스트 알림

---

## 예상 작업량

| 페이지 | 예상 라인 수 | 복잡도 | 예상 시간 |
|--------|-------------|--------|-----------|
| layout.tsx | 120줄 | Low | 1시간 |
| places/page.tsx | 280줄 | Medium | 3시간 |
| places/[id]/page.tsx | 330줄 | Medium-High | 4시간 |
| promotions/page.tsx | 430줄 | High | 5시간 |
| **합계** | **~1,160줄** | - | **~13시간** |

---

## 테스트 시나리오

### 업장 관리
1. [ ] 업장 목록 조회
2. [ ] 업장 통계 정확성 확인
3. [ ] 업장 상세 통계 조회
4. [ ] 평점 분포 시각화 확인
5. [ ] 빈 상태 UI 확인

### 프로모션 관리
1. [ ] 프로모션 생성 (유효성 검증)
2. [ ] 프로모션 수정
3. [ ] 프로모션 삭제 (확인 모달)
4. [ ] 프로모션 활성화/비활성화 토글
5. [ ] 업장별 필터링
6. [ ] 상태별 필터링
7. [ ] 프로모션 상태 뱃지 (진행중/예정/종료)
8. [ ] 날짜 유효성 검증 (종료일 > 시작일)

---

## 다음 단계

✅ **Frontend: 사업자 페이지 라우팅 구조 확인** - 완료!

⏭️ **다음 작업**:
1. `/business/layout.tsx` 구현
2. `/business/places/page.tsx` 구현
3. `/business/places/[id]/page.tsx` 구현
4. `/business/promotions/page.tsx` 구현
